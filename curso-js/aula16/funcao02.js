function soma(n1=0, n2=1) { // n1=0 ou igual a outro número, tanto faz.
    return n1 + n2
}
console.log(soma( 7, 8)) // com esse código, podemos fazer a chamada da função sem dizer um dos parâmetros e mesmo assim dará certo.