function parimpar(n) {
    if (n%2 == 0) {
        return "Par!"
    } else {                            // essa estrutura do if e do else é a ação.
        return "Ímpar!"
    }
}
console.log(parimpar(456)) //parimpar() é o chamado. Enquanto que o valor de n é o parâmetro.
